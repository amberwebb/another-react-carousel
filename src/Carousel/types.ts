export interface CarouselInterface {
  autoScroll: boolean;
  autoScrollInterval: number;
  children: any;
}

export interface CarouselItemInterface {
  index: number;
  activeIndex: number;
  item: any;
}
